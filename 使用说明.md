# 银行金融科技前沿技术研究成果 · 电子研究书 使用说明

一本**可翻页的电子研究书**，纯静态、零依赖、离线可用，用于集中展示银行金融科技领域前沿技术研究成果。

## 一、快速开始

1. 双击根目录下的 `index.html` 即可打开（无需联网、无需服务器）。
2. 首次进入是**封面**，可用以下方式翻阅：
   - 点击书页左右边缘 / 左右箭头按钮 / 键盘 ← → 翻页；
   - 顶栏「📖 书籍 / 🌐 网页 / 🌳 科技树」切换三种阅读方式；「🌓 主题」切换明暗配色；
   - 顶栏搜索框可检索技术名称/关键词，直达对应专题。

> 若某浏览器对 `file://` 限制较严导致异常，可双击 `start.bat`（或运行 `npx serve` / `python -m http.server`）通过本地服务打开。

## 二、三种阅读方式

| 方式 | 说明 |
|------|------|
| 📖 书籍 | 传统的 CSS 3D 翻书模式，封面→扉页→目录→第一篇→第二篇（36 项技术）→附录→封底 |
| 🌐 网页 | 左侧**目录** + 右侧**可滚动全部内容**；目录条目点击即可快速定位，滚动时自动高亮当前章节 |
| 🌳 科技树 | 梳理 36 项技术间「前提技术 → 依赖技术」的层次关系，节点不限于长名单（含基础底座） |

## 三、长名单与分层

- 长名单共 **36 项**，技术名称以《前沿技术储备库分层台账 v1.30》为准；
- 覆盖 **人工智能、数据要素、量子科技、网络安全、基础设施** 五大战略方向；
- 按「**布局层(6) / 论证层(10) / 研究层(10) / 观察层(10)**」四层动态滚动管理；
- 每项含完整台账字段（定义、颠覆性趋势、对银行的价值、当前局限性、六维评级〔技术成熟度/战略匹配度/价值贡献度/引入可行度/战略紧迫度/生态开放度，1–5 分制〕及研判依据、研判结论等）。

## 四、专题研究（4 篇已深化）

| 编号 | 技术 | 层级 | 原始文件 |
|------|------|------|----------|
| T10 | 算力网络（Compute Network） | 研究层 | assets/docs/ZT010/*.docx |
| T18 | 人工智能安全平台（AISP / AI TRiSM） | 论证层 | assets/docs/ZT018/*.docx |
| T31 | 业务编排与自动化（BOAT） | 论证层 | assets/docs/ZT031/*.docx |
| T33 | 决策智能平台（DIP） | 论证层 | assets/docs/ZT033/*.docx |

> 查看完整专题弹窗已**放大至接近全屏并自适应窗口**：顶部为技术简介，下方为「一张图概述 / 评估表 / Word 报告 / PPT 报告」标签，**点击标签直接切换对应内容**（Word/PPT 预览按需加载、不卡顿）；预览区支持**全屏播放**与「新窗口打开」，报告文件可下载。

## 五、目录结构

```
前沿技术研究/
├─ index.html            # 主入口（CSS 内联，加载 data.js 与 js/app.js）
├─ index-standalone.html # 打包后的单文件版（CSS/JS/数据全内联）
├─ data.js               # ★ 内容数据（36 项长名单、图谱、科技树、情报源、技术专题）
├─ js/app.js             # 应用逻辑（书籍/网页/科技树三视图、翻书、图谱、科技树渲染）
├─ assets/
│  ├─ images/            # 一张图概述（SVG）
│  ├─ docs/              # 原始 Word/PPT（按技术分文件夹，供下载）
│  ├─ preview/           # 转换出的 PDF（供在线预览）
│  └─ TZ000_前沿技术储备库分层台账_v1.30.xlsx  # 长名单来源台账
└─ scripts/
   ├─ convert.ps1        # Word/PPT → PDF 批量转换（本机 Office 环境用）
   ├─ docx2html.ps1      # 无 Office 环境下 docx → HTML/文本 提取
   ├─ build_real_assets.ps1 # 为真实专题报告生成 PPT 预览 PDF
   ├─ make_downloads.ps1 # 生成 docx/pptx 下载文件
   ├─ print_pdfs.ps1     # Chrome 打印 HTML → PDF
   └─ pack.ps1           # 打包单文件版 index-standalone.html
```

## 六、接入真实材料（后续新增报告时）

1. **放文件**：将新报告的 Word/PPT 放入 `assets/docs/<技术ID>/`。
2. **改数据**：在 `data.js` 的 `ITEMS` 追加对应条目（含 32 台账字段与六维评级），
   并在条目上补充 `reportDocx / reportPdf / slidesPptx / slidesPdf / image` 路径。
3. **生成预览**：运行 `docx2html.ps1`（提取正文）→ Chrome 打印 PDF；或在本机（装有
   Office）运行 `convert.ps1` 获得高保真转换。

> 说明：Word/PPT 的在线预览采用「离线预转 PDF + 浏览器内嵌预览」方案，跨端一致、无需在阅读端安装 Office。

## 七、技术说明

- 纯原生 HTML/CSS/JavaScript + SVG，**无任何第三方库、无 CDN、无构建**；
- 翻书效果为 CSS 3D 实现，图谱为自研力导向布局，科技树为自研分层树布局，图表为 SVG 绘制；
- **每次翻页翻开一整张跨页**（左右两侧均为新内容）；
- **版面自适应浏览器区域**：书页随窗口缩放，内容超页时自动等比缩小（目录页为可滚动）；
- 桌面为双页书，窄屏（<900px）自动切换为单页模式；
- 数据以 `window.DATA` 全局对象注入，便于二次开发与数据导入导出。

## 八、常见问题

- **预览 PDF 空白**：检查 `assets/preview/<ID>/*.pdf` 是否存在；不存在则运行 `convert.ps1` 生成。
- **下载无反应**：确认 `assets/docs/<ID>/report.docx` 等文件存在。
- **中文乱码（PowerShell 脚本）**：脚本已保存为 UTF-8 with BOM；如重新编辑请保持 BOM。
